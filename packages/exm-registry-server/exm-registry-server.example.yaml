publicUrl: http://localhost:4873/

listen:
  host: 0.0.0.0
  port: 4873

storage:
  kind: file
  root: /data

# Optional Nexus Raw backend. Keep storage.kind set to nexus when using it.
# storage:
#   kind: nexus
# nexus:
#   baseUrl: http://nexus.bluesquall.local/
#   metadataRepository: exm-registry
#   artifactRepository: exm-artifacts
#   username: exm-publisher
#   # Prefer EXM_NEXUS_PASSWORD or EXM_NEXUS_TOKEN for secrets.
